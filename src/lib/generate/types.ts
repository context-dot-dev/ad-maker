export type Brand = {
  domain: string;
  name: string;
  description: string;
  pageContext: string;
  colors: string[];
  colorNames: string[];
};

export type Copy = {
  headline: string;
  sub: string;
};
