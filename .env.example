# Context.dev API key — https://link.context.dev/branda
CONTEXT_DEV_API_KEY=

# AI provider (pick one — the gateway key wins if both are set)
# Vercel AI Gateway — https://vercel.com/docs/ai-gateway
AI_GATEWAY_API_KEY=
# or OpenAI directly
OPENAI_API_KEY=
